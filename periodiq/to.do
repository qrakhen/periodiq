#   interactive: elements that communicate with the main thread via events.
    implement generic events that are extendable

    fix window size problem

    make partial updates possible (Core.updateViewElement)
